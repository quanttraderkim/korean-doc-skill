interface:
  display_name: "Korean Doc Skill"
  short_description: "한국어 비즈니스 문서를 보고용 메모체와 구조화 중심으로 다듬는 가이드"
  default_prompt: "Use $korean-doc-skill to rewrite this Korean business document in concise report-style memo wording with stronger structure, a strict length budget (label sections max 3 bullets, output no longer than the source), tables only where useful, and numbering when helpful."
